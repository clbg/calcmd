{
  "name": "@calcmd/root",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @calcmd/core build",
    "dev": "pnpm --filter @calcmd/core build && pnpm --filter @calcmd/playground dev",
    "dev:website": "pnpm --filter @calcmd/core build && pnpm --filter @calcmd/website dev",
    "build:website": "pnpm --filter @calcmd/core build && pnpm --filter @calcmd/website build",
    "test": "pnpm --filter @calcmd/core test"
  }
}
