# Release Process

## Automated npm Publishing via GitHub Actions

CalcMD uses GitHub Actions with npm's Trusted Publisher feature to automatically publish packages when a version tag is pushed. This is more secure than using access tokens.

## Prerequisites

### 1. Create npm Account

If you don't have an npm account:
```bash
npm adduser
```

### 2. Set Up Trusted Publisher (Recommended)

npm's Trusted Publisher uses GitHub's OIDC to authenticate, eliminating the need for long-lived tokens.

**Steps**:

1. **Publish the package manually once** (required for first-time setup):
   ```bash
   npm login
   cd packages/core
   npm publish --access public
   ```

2. **Go to npm package settings**:
   - Visit: https://www.npmjs.com/package/@calcmd/core/access
   - Or: npm website → Your package → Settings → Publishing Access

3. **Add Trusted Publisher**:
   - Click "Add Trusted Publisher"
   - Select "GitHub Actions"
   - Fill in:
     - **Repository owner**: `clbg`
     - **Repository name**: `calcmd`
     - **Workflow name**: `publish-npm.yml`
     - **Environment** (optional): leave blank
   - Click "Add"

4. **Done!** No token needed. GitHub Actions can now publish automatically using OIDC.

**Important Notes**:
- The workflow uses `npm publish` (not `pnpm publish`) because pnpm doesn't support OIDC authentication yet
- Provenance attestation is automatically generated by npm CLI (no `--provenance` flag needed)
- Node.js 24 is used to ensure npm CLI 11.5.1+ for trusted publishing support

## Release Steps

### Automated Release (Recommended)

1. **Update version** in `packages/core/package.json`:
   ```json
   {
     "version": "0.1.1"
   }
   ```

2. **Commit the version bump**:
   ```bash
   git add packages/core/package.json
   git commit -m "chore: bump @calcmd/core to v0.1.1"
   ```

3. **Create and push a git tag**:
   ```bash
   git tag v0.1.1
   git push origin v0.1.1
   ```

4. **GitHub Actions will automatically**:
   - Install dependencies with pnpm
   - Build the package
   - Run tests
   - Publish to npm using OIDC authentication
   - Generate provenance attestation

5. **Monitor the workflow**:
   - Go to GitHub → Actions tab
   - Watch the "Publish to npm" workflow
   - Check for any errors

### Manual Release (Fallback)

If you prefer to publish manually:

1. **Ensure you're logged in to npm**:
   ```bash
   npm whoami
   # If not logged in:
   npm login
   ```

2. **Build and test**:
   ```bash
   pnpm build
   pnpm test
   ```

3. **Publish**:
   ```bash
   cd packages/core
   npm publish --access public
   ```

4. **Create git tag** (for consistency):
   ```bash
   git tag v0.1.1
   git push origin v0.1.1
   ```

## Version Numbering

Follow [Semantic Versioning](https://semver.org/):

- **MAJOR** (1.0.0): Breaking changes
- **MINOR** (0.1.0): New features, backward compatible
- **PATCH** (0.0.1): Bug fixes, backward compatible

### Examples

- `0.1.0` → `0.1.1`: Bug fix
- `0.1.0` → `0.2.0`: New feature (e.g., new function)
- `0.1.0` → `1.0.0`: Breaking change (e.g., API change)

## Pre-release Versions

For beta/alpha releases:

```bash
# Update version to 0.2.0-beta.1
git tag v0.2.0-beta.1
git push origin v0.2.0-beta.1
```

Users can install with:
```bash
npm install @calcmd/core@beta
```

## Troubleshooting

### "Package already exists"

If the version is already published:
1. Bump the version number
2. Commit and create a new tag

### "ENEEDAUTH" Error

This means npm authentication failed. For trusted publishing:
1. Verify Trusted Publisher is configured on npm
2. Check workflow has `id-token: write` permission
3. Ensure workflow uses `npm publish` (not `pnpm publish`)
4. Verify Node.js version is 22+ in workflow

### "Tests failed"

Fix the failing tests before releasing:
```bash
pnpm test
```

### "Build failed"

Ensure the build works locally:
```bash
pnpm build
```

## Rollback

If you need to unpublish a version (within 72 hours):

```bash
npm unpublish @calcmd/core@0.1.1
```

**Warning**: Unpublishing is discouraged. Instead, publish a new patch version with the fix.

## Verification

After publishing, verify:

1. **Check npm**:
   ```bash
   npm view @calcmd/core
   ```

2. **Test installation**:
   ```bash
   mkdir test-install
   cd test-install
   npm init -y
   npm install @calcmd/core
   node -e "console.log(require('@calcmd/core'))"
   ```

3. **Check provenance**:
   - Go to https://www.npmjs.com/package/@calcmd/core
   - Look for "Provenance" badge
   - Click to see build attestation

## Release Checklist

Before creating a release tag:

- [ ] All tests pass (`pnpm test`)
- [ ] Build succeeds (`pnpm build`)
- [ ] Version bumped in `package.json`
- [ ] README.md updated (if needed)
- [ ] Breaking changes documented
- [ ] Examples updated (if API changed)

After release:

- [ ] Verify package on npm
- [ ] Test installation in fresh project
- [ ] Create GitHub release with notes
- [ ] Announce on social media

---

**Current Status**: Trusted Publisher configured, automated publishing via GitHub Actions
